/**
 * Key used to store and retrieve the user object from the request object.
 * @constant {string}
 */
export const REQUEST_USER_KEY = 'user';

/**
 * Key used to store and retrieve the role types for role-based access control.
 * @constant {string}
 */
export const ROLE_TYPE_KEY = 'RoleType';

/**
 * Number of salt rounds used for hashing passwords with bcrypt.
 * @constant {number}
 */
export const SALT_ROUNDS = 10;
